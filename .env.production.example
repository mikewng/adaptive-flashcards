# Production Environment Variables
# Copy this to .env on your EC2 instance and fill in the values

# Domain Configuration (optional - only needed if using custom domain with HTTPS)
DOMAIN_NAME=yourdomain.com

# SSL Configuration (optional - only needed if using custom domain)
SSL_EMAIL=your-email@example.com

# Database Configuration
POSTGRES_PASSWORD=your-secure-database-password
DATABASE_URL=postgresql+psycopg://postgres:your-secure-database-password@db:5432/flashcards

# JWT Configuration
JWT_SECRET=your-jwt-secret-generated-with-openssl

# API URL Configuration
# IMPORTANT: Include /api at the end!
# If using EC2 IP without domain:
NEXT_PUBLIC_API_URL=http://YOUR_EC2_IP:8000/api
# If using custom domain with HTTPS:
# NEXT_PUBLIC_API_URL=https://yourdomain.com/api

# CORS Configuration
# If using EC2 IP:
CORS_ORIGINS='["http://YOUR_EC2_IP:3000", "http://localhost:3000"]'
# If using custom domain:
# CORS_ORIGINS='["https://yourdomain.com", "https://www.yourdomain.com"]'
